# Stable ingress for vote app - QA environment
# Argo Rollouts will manage canary annotations automatically
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting-app
  labels:
    environment: qa
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - opsera-voting-app-enterprise-2-qa.agent.opsera.dev
    secretName: voting-app-enterprise-2-qa-tls
  rules:
  - host: opsera-voting-app-enterprise-2-qa.agent.opsera.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vote
            port:
              number: 80
