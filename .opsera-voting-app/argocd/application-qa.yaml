apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting-app-enterprise-2-qa
  namespace: argocd
  labels:
    app.kubernetes.io/name: voting-app-enterprise-2
    app.kubernetes.io/part-of: voting-app-enterprise-2
    environment: qa
    deployment-strategy: canary
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: deployments
    notifications.argoproj.io/subscribe.on-sync-failed.slack: deployments
    notifications.argoproj.io/subscribe.on-health-degraded.slack: alerts
    # Canary-specific notifications
    notifications.argoproj.io/subscribe.on-rollout-step-completed.slack: deployments
    notifications.argoproj.io/subscribe.on-rollout-aborted.slack: alerts
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/Opsera-Test-M/voting-app-enterprise-1.git
    targetRevision: main
    path: .opsera-voting-app/k8s/overlays/qa

  destination:
    # Use cluster name, not server URL (RULE 10)
    name: opsera-usw2-np
    namespace: voting-app-enterprise-2-qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      # Required for Argo Rollouts
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Ignore differences for Rollout status fields (managed by Argo Rollouts)
  ignoreDifferences:
    - group: argoproj.io
      kind: Rollout
      jsonPointers:
        - /spec/replicas
        - /status
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /spec/volumeClaimTemplates/0/apiVersion
        - /spec/volumeClaimTemplates/0/kind
